import { EmptyPipePipe } from './empty-pipe.pipe';

describe('EmptyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmptyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
